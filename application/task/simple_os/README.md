# Simple OS - 轻量级裸机任务调度器

## 📖 简介

Simple OS 是一个轻量级的裸机任务调度器，专为 STM32 等嵌入式系统设计。它提供了简单易用的周期性任务调度功能，无需 RTOS 即可实现多任务管理。

### ✨ 特性

- 🚀 **轻量级**：无需 RTOS，零开销的任务调度
- 📦 **静态内存分配**：最多支持 16 个任务，无动态内存分配
- ⏱️ **周期性调度**：基于 HAL_GetTick() 的精确时间控制
- 🎯 **优先级支持**：数值越小优先级越高（0 = 最高优先级）
- 🔄 **任务管理**：支持启用/禁用任务、动态修改周期
- 🔧 **C/C++ 双接口**：同时支持 C 和 C++ 调用方式

---

## 🚀 快速开始

### 1. C++ 接口使用（推荐）

```cpp
#include "task/simple_os/scheduler.h"

// 定义任务函数
void task1(void) {
    // 任务 1 的代码
    uart_log.printf("Task 1 running\r\n");
}

void task2(void) {
    // 任务 2 的代码
    uart_log.printf("Task 2 running\r\n");
}

// 创建全局调度器实例
Scheduler os;

int main(void) {
    // 硬件初始化
    HAL_Init();
    SystemClock_Config();

    // 初始化调度器
    os.init();

    // 添加任务
    // 参数：任务函数, 周期(ms), 优先级(可选，默认0)
    os.addTask(task1, 100, 1);  // 每 100ms 执行一次，优先级 1
    os.addTask(task2, 500, 2);  // 每 500ms 执行一次，优先级 2

    // 主循环
    while (1) {
        os.run();  // 运行调度器
    }
}
```

### 2. C 语言接口使用

```c
#include "task/simple_os/scheduler.h"

// 定义任务函数
void task1(void) {
    printf("Task 1 running\r\n");
}

void task2(void) {
    printf("Task 2 running\r\n");
}

int main(void) {
    // 硬件初始化
    HAL_Init();
    SystemClock_Config();

    // 创建调度器句柄
    SchedulerHandle scheduler = scheduler_create();

    // 初始化调度器
    scheduler_init(scheduler);

    // 添加任务
    scheduler_add_task(scheduler, task1, 100, 1);  // 每 100ms 执行
    scheduler_add_task(scheduler, task2, 500, 2);  // 每 500ms 执行

    // 主循环
    while (1) {
        scheduler_run(scheduler);  // 运行调度器
    }
}
```

---

## 📚 API 参考

### C++ 类接口

#### `Scheduler` 类

##### 构造函数
```cpp
Scheduler();
```
创建调度器实例。

##### `void init()`
初始化调度器，清空所有任务。**必须在添加任务前调用**。

##### `bool addTask(void (*task_func)(void), uint32_t period_ms, uint8_t priority = 0)`
添加任务到调度器。

**参数：**
- `task_func`：任务函数指针（无参数、无返回值）
- `period_ms`：任务执行周期（毫秒）
- `priority`：任务优先级（0-255，数值越小优先级越高，默认 0）

**返回值：**
- `true`：添加成功
- `false`：任务列表已满（最多 16 个任务）

**示例：**
```cpp
os.addTask(motor_task, 5, 1);    // 5ms 周期，优先级 1
os.addTask(uart_task, 10, 2);    // 10ms 周期，优先级 2
os.addTask(led_task, 1000);      // 1000ms 周期，默认优先级 0
```

##### `void run()`
运行调度器，检查并执行到期的任务。**在主循环中持续调用**。

**示例：**
```cpp
while (1) {
    os.run();
}
```

##### `uint8_t getTaskCount()`
获取当前任务数量。

**返回值：** 当前已添加的任务数量（0-16）

##### `void clearAllTasks()`
清空所有任务。

##### `void enableTask(uint8_t index)`
启用指定索引的任务。

**参数：**
- `index`：任务索引（0-15）

##### `void disableTask(uint8_t index)`
禁用指定索引的任务（不会被执行）。

**参数：**
- `index`：任务索引（0-15）

##### `void setTaskPeriod(uint8_t index, uint32_t period_ms)`
动态修改任务的执行周期。

**参数：**
- `index`：任务索引（0-15）
- `period_ms`：新的周期（毫秒）

**示例：**
```cpp
os.setTaskPeriod(0, 200);  // 将第 0 个任务的周期改为 200ms
```

---

### C 语言接口

#### `SchedulerHandle scheduler_create(void)`
创建调度器实例。

**返回值：** 调度器句柄

#### `void scheduler_init(SchedulerHandle handle)`
初始化调度器。

#### `int scheduler_add_task(SchedulerHandle handle, void (*task_func)(void), uint32_t period_ms, uint8_t priority)`
添加任务。

**返回值：**
- `1`：成功
- `0`：失败

#### `void scheduler_run(SchedulerHandle handle)`
运行调度器。

#### `uint8_t scheduler_get_task_count(SchedulerHandle handle)`
获取任务数量。

#### `void scheduler_enable_task(SchedulerHandle handle, uint8_t index)`
启用任务。

#### `void scheduler_disable_task(SchedulerHandle handle, uint8_t index)`
禁用任务。

#### `void scheduler_set_task_period(SchedulerHandle handle, uint8_t index, uint32_t period_ms)`
设置任务周期。

#### `void scheduler_destroy(SchedulerHandle handle)`
销毁调度器实例。

---

## 💡 使用示例

### 示例 1：电机控制 + 串口通信

```cpp
#include "task/simple_os/scheduler.h"

void motor_task_proc(void) {
    // 电机控制逻辑（高频任务）
    motor_control_update();
}

void uart_task_proc(void) {
    // 串口通信处理（低频任务）
    uart_process_commands();
}

Scheduler os;

int main(void) {
    HAL_Init();
    SystemClock_Config();

    // 初始化硬件
    MX_GPIO_Init();
    MX_UART7_Init();
    MX_FDCAN1_Init();

    // 初始化调度器
    os.init();

    // 添加任务
    os.addTask(motor_task_proc, 5, 1);   // 5ms 周期，高优先级
    os.addTask(uart_task_proc, 50, 2);   // 50ms 周期，低优先级

    while (1) {
        os.run();
    }
}
```

### 示例 2：动态任务管理

```cpp
Scheduler os;

void fast_task(void) {
    // 快速任务
}

void slow_task(void) {
    // 慢速任务
}

int main(void) {
    HAL_Init();
    os.init();

    // 添加任务
    os.addTask(fast_task, 10, 0);   // 任务索引 0
    os.addTask(slow_task, 1000, 1); // 任务索引 1

    uint32_t counter = 0;

    while (1) {
        os.run();

        counter++;

        // 运行 5 秒后禁用快速任务
        if (counter == 500) {
            os.disableTask(0);
        }

        // 运行 10 秒后重新启用并修改周期
        if (counter == 1000) {
            os.enableTask(0);
            os.setTaskPeriod(0, 50);  // 改为 50ms 周期
        }

        HAL_Delay(10);
    }
}
```

---

## ⚙️ 工作原理

### 调度机制

1. **时间基准**：使用 `HAL_GetTick()` 获取系统时间（1ms 精度）
2. **周期检查**：每次调用 `run()` 时，检查每个任务是否到达执行时间
3. **优先级排序**：任务按优先级排序，优先级高的任务先执行
4. **非阻塞执行**：任务函数应快速返回，避免阻塞调度器

### 性能优化

- ✅ **快速路径优化**：如果系统时间未变化，直接返回
- ✅ **插入排序**：添加任务时自动按优先级排序
- ✅ **静态分配**：无动态内存分配，避免内存碎片
- ✅ **槽位复用**：删除任务后槽位可被复用

---

## ⚠️ 注意事项

### 1. 任务函数要求
- ✅ 任务函数必须**快速返回**（建议 < 1ms）
- ❌ 不要在任务函数中使用 `HAL_Delay()` 等阻塞函数
- ✅ 使用状态机或计数器实现复杂逻辑

**错误示例：**
```cpp
void bad_task(void) {
    HAL_Delay(100);  // ❌ 阻塞调度器！
    do_something();
}
```

**正确示例：**
```cpp
void good_task(void) {
    static uint32_t counter = 0;
    counter++;

    if (counter >= 10) {  // 每 10 次执行一次
        do_something();
        counter = 0;
    }
}
```

### 2. 周期设置建议
- 最小周期：≥ 1ms（受 `HAL_GetTick()` 精度限制）
- 高频任务：1-10ms（如电机控制、传感器读取）
- 中频任务：10-100ms（如通信处理、状态更新）
- 低频任务：100-1000ms（如 LED 闪烁、日志输出）

### 3. 优先级规则
- 数值越小优先级越高（0 = 最高优先级）
- 相同优先级的任务按添加顺序执行
- 建议为不同类型的任务分配不同优先级

### 4. 任务数量限制
- 最多支持 **16 个任务**
- 超过限制时 `addTask()` 返回 `false`

---

## 🔧 配置选项

### 修改最大任务数量

编辑 `scheduler.h` 文件：

```cpp
class Scheduler {
private:
    static constexpr uint8_t MAX_TASKS = 32;  // 修改为 32
    // ...
};
```

**注意：** 增加任务数量会增加内存占用（每个任务约 16 字节）。

---

## 📊 内存占用

- **Scheduler 对象**：约 300 字节（16 个任务）
- **每个任务**：约 16 字节
- **总计**：< 1KB（适合资源受限的嵌入式系统）

---

## 🆚 与 RTOS 对比

| 特性 | Simple OS | FreeRTOS/RT-Thread |
|------|-----------|-------------------|
| 内存占用 | < 1KB | > 10KB |
| 学习曲线 | 简单 | 中等 |
| 任务切换 | 协作式 | 抢占式 |
| 实时性 | 软实时 | 硬实时 |
| 适用场景 | 简单应用 | 复杂应用 |

---

## 📝 完整示例（main.cpp）

```cpp
#include "main.h"
#include "bsp_system.h"

void SystemClock_Config(void);

Uart_Instance uart_log(&huart7, true);

// 任务函数定义
void motor_task_proc(void) {
    // 电机控制逻辑
    static uint32_t count = 0;
    count++;
    if (count % 200 == 0) {  // 每 1 秒打印一次
        uart_log.printf("Motor task: %lu\r\n", count);
    }
}

void uart_task_proc(void) {
    // 串口处理逻辑
    uart_log.printf("UART task running\r\n");
}

// 创建全局调度器
Scheduler os;

int main(void) {
    // 硬件初始化
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_UART7_Init();
    MX_FDCAN1_Init();

    HAL_Delay(100);

    // 初始化串口
    uart_log.init();
    uart_log.printf("\r\n=== System Starting ===\r\n");

    // 配置 FDCAN
    bsp_fdcan_set_baud(&hfdcan1, CAN_CLASS, CAN_BR_1M);
    bsp_can_init();

    // 初始化调度器
    os.init();

    // 初始化任务
    motor_task_init();
    uart_task_init();

    // 添加任务到调度器
    os.addTask(motor_task_proc, 5, 1);   // 5ms 周期，优先级 1
    os.addTask(uart_task_proc, 50, 2);   // 50ms 周期，优先级 2

    uart_log.printf("Scheduler running...\r\n\r\n");

    HAL_Delay(500);

    // 主循环
    while (1) {
        os.run();  // 运行任务调度器
    }
}
```

---

## 🐛 常见问题

### Q1: 任务没有按预期周期执行？
**A:** 检查任务函数是否有阻塞操作（如 `HAL_Delay()`），这会影响调度器的运行。

### Q2: 添加任务失败？
**A:** 检查是否超过最大任务数量（16 个），或者任务函数指针是否为 `nullptr`。

### Q3: 如何实现精确的时间控制？
**A:** Simple OS 基于 `HAL_GetTick()`（1ms 精度），适合软实时应用。如需更高精度，考虑使用硬件定时器。

### Q4: 可以在中断中调用 `os.run()` 吗？
**A:** 不建议。调度器应在主循环中调用，中断中只做必要的标志位设置。

---

## 📄 许可证

本项目遵循 MIT 许可证。

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**Happy Coding! 🚀**
